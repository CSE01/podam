general:
  build_dir: api
  
test:
  post:
    - mkdir -p $CIRCLE_TEST_REPORTS/junit/
    - find . -type f -regex ".*/podam/src/main/resources/META-INF/spring/.*xml" -exec cp {} $CIRCLE_TEST_REPORTS/junit/ \;
